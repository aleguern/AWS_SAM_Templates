type Velo {
  id: ID!
}
type Query {
  getVelo(id: ID!): Velo
}
type Mutation {
  createVelo: Velo
}

type Schema {
  query: Query
}

type Subscription {
  veloCreated: Velo @aws_subscribe(mutations: ["createVelo"])
}
