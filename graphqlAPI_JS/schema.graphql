type Schema {
  mutation: Mutation
  query: Query
}
type Mutation {
  createList(list: ListInput!): List
}
type Query {
  getList(id: ID!): List
}
input ListInput {
  title: String
}
type List { 
  id: String
  title: String
}
